version: '3'

services:
  favorite_db:
    image: postgres
    ports:
      - "4002:5432"
  favorite_ms:
    build: .
    command: python3 manage.py runserver 0.0.0.0:3302
    volumes:
      - .:/code
    ports:
      - "3302:3302"
    depends_on:
      - favorite_db